DATABASE_URL="postgresql://user:password@host:5432/pennywise?schema=public"

# ============================================
# JWT AUTHENTICATION
# ============================================
# Generate secure random strings for JWT secrets
# You can use: openssl rand -base64 32
# Or online: https://randomkeygen.com/
# Pre-generated secrets (you can regenerate for production):
JWT_ACCESS_SECRET="hSymI1zphZRP2Pi67WkHLKgQ8+/HyiG6sJNNfW/99DY="
JWT_REFRESH_SECRET="wv5JFri9N8CPutATRq5PK0hVDmvzoc3VSvvl4rIz2KI="

# JWT Token expiration times (in seconds or string format like "15m", "7d")
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=3001
NODE_ENV="development"

# CORS Configuration (comma-separated origins for production)
# For development: http://localhost:3000
# For production: https://yourdomain.com
CORS_ORIGIN="http://localhost:3000"

# ============================================
# LOGGING
# ============================================
LOG_LEVEL="info"

# ============================================
# REDIS (Optional - for caching)
# ============================================
# If using Redis for caching:
# 1. Sign up at https://redis.com/cloud/ (free tier available)
#    OR https://upstash.com/ (serverless Redis, free tier)
#    OR use local Redis: redis-server
#
# 2. For Upstash:
#    - Create a database
#    - Copy the REST URL and Token
#
# REDIS_URL="redis://localhost:6379"
# REDIS_PASSWORD=""

# ============================================
# NOTES
# ============================================
# 1. Copy this file to .env: cp env.example .env
# 2. Fill in all the values above
# 3. NEVER commit .env to git (it's in .gitignore)
# 4. Run: npm run prisma:migrate (to create database tables)
# 5. Run: npm run prisma:generate (to generate Prisma client)
